{
  "name": "knowher-api",
  "private": true,
  "version": "0.0.0",
  "description": "knowHer API — Node + Express + TypeScript. All business logic and cycle math live here.",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^18.0.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.19.43",
    "typescript": "^7.0.2"
  }
}
